import { CarBaseSearchPipe } from './car-base-search.pipe';

describe('CarBaseSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CarBaseSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
